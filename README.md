midi for netlogo
=============

This extensions for NetLogo (cite/ref needed) aims to provide NetLogo with
full support of midi commands

Installing
-------
NetLogo will look for extensions in several places:
* In the folder of the current model.
* The extensions folder in the same location as the NetLogo application. 
(From the official documentation http://ccl.northwestern.edu/netlogo/5.0/docs/extensions.html)

In order to install the extension just place the content of the provided 
zip-Files it in one of the given locations.

Using
-------
In order to make the extension available in your model just place
`extensions [midi]`
on top of your code.
